
class Solution:
    def addBinary(self, a: str, b: str) -> str:
        # Bin adds "0b" as prfix befort output string. 
        
        return str(bin(int(a,2) + int(b,2)))[2:]
    
# Working first try solution no help required. This was really cool.
# Here we learned about Bin and its ability to convert integers to binary

sol = Solution()

sol.addBinary("1010","1011")
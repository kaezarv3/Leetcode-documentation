

class Solution:
    def threeConsecutiveOdds(self, arr: List[int]) -> bool:
        count = 0
        for i in arr:
            if i % 2 != 0:
                count += 1
            else:
                count = 0
            if count == 3:
                return True
        return False

# Solved First try using intuition, I dont think it is the most optimal but it works 

